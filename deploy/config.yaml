server:
  host: "0.0.0.0"
  port: 8080
  readTimeout: 60
  writeTimeout: 60
  adminToken: "go-diamond-admin-token"

database:
  dsn: "root:root@tcp(127.0.0.1:3306)/go_diamond?charset=utf8mb4&parseTime=True"
  maxOpenConns: 20
  maxIdleConns: 10
  connMaxLifetime: 300

notifier:
  pollInterval: 2

log:
  level: "info"
  format: "json"

node:
  id: ""