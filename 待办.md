# go-diamond 待办事项

## 高可用性

### SDK 本地缓存 + 降级机制

**问题背景：**
当前 SDK (`pkg/diamond/`) 未实现本地缓存，服务端单点故障会影响客户端的可用性。

**目标：**
实现 SDK 客户端的本地缓存降级能力，支持：
1. 启动时拉取配置并缓存到本地文件
2. 服务运行中长轮询监听配置变更，实时更新
3. 服务端挂了时读取本地缓存继续运行（只读）
4. 服务端恢复后自动重连并继续监听

**优先级：** 高

**状态：** 待实现

---

## 多语言客户端完善

### 当前进度
- Go SDK: `pkg/diamond/`
- Python 示例: `examples/python/`
- Java 示例: `examples/java/`

### 待完善
- [ ] Python SDK 完善（增加本地缓存支持）
- [ ] Java SDK 完善（增加本地缓存支持）
- [ ] 统一 SDK 接口规范

---

## 高可用集群

### 目标
支持多实例部署，消除单点故障。

### 方案待定
- [ ] 主备模式（2台 + DNS切换）
- [ ] 集群模式 + Keepalived VIP漂移
- [ ] K8s 原生支持

**前置依赖：** SDK 本地缓存机制实现

---

## 其他优化

- [ ] 配置格式校验增强（JSON/YAML/XML）
- [ ] 权限管理（namespace 级别）
- [ ] 配置变更通知（支持 Webhook）
- [ ] 审计日志